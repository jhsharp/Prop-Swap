/// @description Set Volumes

audio_sound_gain(snWalk, 0.5, 0);
audio_sound_gain(snLanding, 0.3, 0);
audio_sound_gain(snPush, 0.5, 0);
audio_sound_gain(snLandingObject, 0.05, 0);
audio_sound_gain(snLink, 0.05, 0);
audio_sound_gain(snCancel, 0.3, 0);
audio_sound_gain(snShot, 0.4, 0);
audio_sound_gain(snDoorOpen, 0.1, 0);
audio_sound_gain(snDoorClose, 0.5, 0);

audio_sound_gain(snMenu, 0.5, 0);
audio_sound_gain(snArea1, 0.3, 0);
audio_sound_gain(snArea2, 0.3, 0);
audio_sound_gain(snArea3, 0.3, 0);
audio_sound_gain(snCredits, 0.5, 0);